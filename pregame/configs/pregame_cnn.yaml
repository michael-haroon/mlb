source_uri: s3://mlb-265753586044-us-east-1-an/data
feature_store_dir: deep_learning/artifacts/feature_store
model_output_dir: deep_learning/artifacts/pregame_cnn

season_start: 2015
season_end: null

history_length: 20
min_history: 5
batch_size: 64
epochs: 20
learning_rate: 0.0005
weight_decay: 0.0001
hidden_dim: 128
dropout: 0.20

# exp(-lambda * age_days). 0.003 gives roughly half weight after 231 days.
time_decay_lambda: 0.003

